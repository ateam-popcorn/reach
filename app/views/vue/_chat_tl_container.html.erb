<script id="survay-chat-tl-container" type="x-template">
  <div class="comments" style="clear: both; padding-top: 32px">
    <div v-for="c in comments" class="comment" style="margin-bottom: 5px">
      <strong>{{ c.profile.username }}</strong><br>
      {{ c.comment }}
      <hr>
    </div>
  </div>

  <form class="form-horizontal" v-on:submit.prevent="sendMessage">
    <div class="input-group">
      <input v-model="newComment" type="text" class="form-control">
      <span class="input-group-btn">
        <input type="submit" class="btn btn-primary" value="送信">
      </span>
    </div>
  </form>
</script>
